{
  "name": "ngl-wave-premium",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && npx esbuild index.tsx --bundle --outfile=dist/index.js --loader:.tsx=tsx --loader:.ts=ts --minify --platform=browser --format=esm --define:process.env.NODE_ENV='\"production\"' --define:process.env.API_KEY=\"\\\"$API_KEY\\\"\" && cp index.html dist/",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "esbuild": "^0.20.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@google/genai": "^1.41.0",
    "lucide-react": "^0.460.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}